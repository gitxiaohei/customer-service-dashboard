# 客服数据日报看板 - 日期来源修复

## 问题
当前日期选择器的日期是"发送日期"，但实际上每天发送的报告是"前一天的指标"（例如4月24日发送的是4月23日的日报）。

## 修复要求
日期选择器的可用日期，应该来自「数据推送测试」群里机器人实际发送的消息——每条消息里包含的是哪天的数据，就提供那个日期供选择。

实际可用的日期需要根据群里的历史消息来确定（从消息内容中解析出日期）。

数据拉取逻辑：
- 读取群里机器人发送的客服数据日报消息
- 从消息内容中解析出报告对应的日期（如"4月23日"）
- 日期选择器只提供有实际数据的日期
- 选择某日期后，展示该日期对应的数据